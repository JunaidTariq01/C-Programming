#include <stdio.h>
#include<math.h>
int main(){
float base,alt;
float s=0.5;
printf("Enter Altitude of triangle  ");
scanf("%f",&alt);
printf ("Enter Base length ");
scanf("%f",&base);
printf("ARea of triangle is: %f",(s*(base*alt)));
return 0;
}